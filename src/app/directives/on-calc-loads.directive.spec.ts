import { OnCalcLoadsDirective } from './on-calc-loads.directive';

describe('OnCalcLoadsDirective', () => {
  it('should create an instance', () => {
    const directive = new OnCalcLoadsDirective();
    expect(directive).toBeTruthy();
  });
});
